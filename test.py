import unittest
from solution import *


class TestMethods(unittest.TestCase):
    # assert that input floor is an int.
    def test_isupper(self):
        self.assertTrue(floors=int)

        # test call elevator
        def test_call_elevator(self):
            if len(elevators) > 8:
                raise ValueError("maximum of 8 elevators")

        # Test that elevator numbers are limited to 8
        def test_elevator(self):
            self.assertEqual(8, elevators)


if __name__ == '__main__':
    unittest.main()
